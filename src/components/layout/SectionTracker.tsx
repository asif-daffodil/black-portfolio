'use client';

// In the HUD floating panel system, activeSection is orchestrated directly by
// 3D console buttons, keyboard shortcuts (1-8 / arrow keys), swipe gestures,
// and semantic navigation links.
export function SectionTracker() {
  return null;
}
